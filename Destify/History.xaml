<Window x:Class="DestifySharp.History"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="History" Height="269" Width="609" Loaded="windowLoaded" Icon="/DestifySharp;component/Destify.ico" ShowInTaskbar="False">
    <Window.ContextMenu>
        <ContextMenu >
            <MenuItem Header="Clear History" Click="menuItemClick" />
        </ContextMenu>
    </Window.ContextMenu>
    <Grid>
        <DataGrid AutoGenerateColumns="True" HorizontalAlignment="Stretch" Name="dataGrid1" VerticalAlignment="Stretch" ColumnWidth="*">
        </DataGrid>
    </Grid>
</Window>
